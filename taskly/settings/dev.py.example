from .common import *

DEBUG = True

SECRET_KEY = your-secret-key

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        "OPTIONS": {
            "read_default_file": "taskly/settings/my.cnf",
        },
    }
}